// Dán phía trên hàm main

void handleHardDrop() {
    boardDelBlock(); 

    while (canMove(0, 1)) {
        y++;
    }

    block2Board();
    
    soundDrop();
}


// Dán vào trong block check phím
    // Tìm trong hàm main(), đoạn xử lý _kbhit():
    if (_kbhit()) {
        char c = _getch();
        

        // THÊM ĐOẠN NÀY:
        else if (c == ' ') { 
            handleHardDrop();
            
            // Ép game vẽ lại ngay lập tức để thấy kết quả rơi
            draw(); 
        }
    }



// Trong hàm draw

    cout << "W : ROTATE" << endl;
    cout << "SPACE : HARD DROP" << endl; // Thêm dòng này để người dùng biết phím tắt
    cout << "Q : QUIT" << endl;
